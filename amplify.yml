version: 1
env:
  variables:
    NODE_VERSION: '20'
backend:
  phases:
    build:
      commands:
        - npm ci --cache .npm --prefer-offline
        # Compile backend TypeScript explicitly for clearer errors
        - npx tsc -p amplify/tsconfig.json --pretty false || true
        # Generate dictionary JSON from english-words for Lambda to import
        - node scripts/build-dictionary.mjs
        - echo "Running ampx pipeline-deploy with debug logging" 
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID --debug
frontend:
  phases:
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
